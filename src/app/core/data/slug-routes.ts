// base route
// El nombre de la ruta debe ser igual al declarado en el registro
// de aplicacion/route de ContainerRoot(app-root-config SPA)

export const SLUG_TAXPAYER          = "contribuyente";

// View por defecto: Search
export const PATH_SEARCH_TAXPAYER   = `${SLUG_TAXPAYER}`

// View Registrar Contribuyente
export const PATH_REGISTER_TAXPAYER = `${SLUG_TAXPAYER}/registro`;
